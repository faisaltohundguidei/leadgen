# LeadGen

A scalable, async lead generation engine.

## Features
- SERP-based discovery (Google, Bing)
- Async scraping
- CSV / pluggable storage
- CLI + API ready
- No frontend required

## Install
```bash
pip install leadgen
